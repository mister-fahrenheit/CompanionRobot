Installation & Compression:
    1. Install ImageMagick (https://imagemagick.org/script/download.php)
    2. cd into .data_card
    3. for %f in (Uncompressed\*.bmp) do magick "Uncompressed\%~nxf" -type palette -define bmp:format=bmp3 -compress None "Compressed\%~nxf"